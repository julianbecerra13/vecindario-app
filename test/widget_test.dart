import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App se inicializa correctamente', (WidgetTester tester) async {
    // Test placeholder - se completará cuando Firebase esté configurado
    expect(1 + 1, equals(2));
  });
}
